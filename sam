docker commands:
to view containers: docker ps
to view all containers : docker ps -a
to view images : docker images
volumes : docker volume ls
networks: docker network ls
to remove all the containers : docker container prune -f or docker remove $(docker ps -a)
to removes images : docker image prune -y or docker rmi $(docker images)
to remove volumes : docker volume remove volume_name
to clean all the system : docker system prune -f
Dockerfile components:
FROM
LABEL
WORKDIR
COPY
RUN
CMD
ENTRYPOINT
VAR
ARG
to run container : docker run -itd -p port:hport image_name:tag
docker compose :
docker-compose up -d
docker-compose down